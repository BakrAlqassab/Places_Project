<% if(success && success.length) { %>
<div class="flash-success" role="alert"><%= success %></div>
<% } %>

<% if(error && error.length) { %>
<div class="flash-failed" role="alert"><%= error %></div>
<% } %>
