<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Places</title>
</head>
<body>
    <h1> Edit Place</h1>
    <form action="/places/<%= place._id %>?_method=PUT" method='POST'>

        <div>
            <label for='title'>title</label>
            <input type='text' id='title' name='place[title]' value="<%= place.title %>" />
            
        </div>
        <div>
            <label for='location'>location</label>
            <input type='text' id='location' name='place[location]'  value="<%= place.location %>"/>
        </div>
        <button >Update Place</button>
        <a href="/places/<% place._id %>">Back to Place</a>
    </form>
</body>
</html>