<% layout('./layouts/boilerplate') %>

<div class="row">
  '
  <div class="col-6 offset-3">
    <div class="card mb-3">
      <img src="<%= place.image %>" class="img-fluid" alt="..." />
      <div class="card-body">
        <h5 class="card-title"><%= place.title %></h5>
        <p class="card-text"><%= place.description %></p>
      </div>
      <ul class="list-group list-group-flush">
        <li class="text-light"><%= place.location %></li>
      </ul>
      <ul class="list-group list-group-flush">
        <li class="text-light">latitude: <%= place.coordinates[0].lat %> , longtitude:  <%= place.coordinates[0].lng %> </li>
      </ul>
      <div class="card-body">
        <!--      
          <a  class="card-link btn btn-warning"  href="/places/<%= place._id %>/edit"> Edit </a> -->
        <a class="btn btn-info" href="/places/<%= place._id %>/edit">
          Edit
        </a>
        <form
          class="d-inline"
          action="/places/<%= place._id %>?_method=DELETE"
          method="POST"
        >
          <button class="btn btn-danger d-inline">Delete</button>
        </form>
      </div>
    </div>
  </div>
</div>

<br />
<a href="/places"> All Places </a>

<!-- <% layout('./layouts/boilerplate') %>
    <h1>PLACES SHOW</h1>

    <h1><%= place.title %></h1>
    <h2><%= place.location %></h2>
    <img src="<%= place.image %>" alt='' >
    <p><%= place.description %></p>
    <a href="/places/<%= place._id %>/edit"> Edit </a>
    <p>

        <form action="/places/<%= place._id %>?_method=DELETE" method='POST'>
          <button>Delete</button>
        </form>
    </p>
    <footer>
      <a href="/places"> All Places </a>
    </footer>
 -->
